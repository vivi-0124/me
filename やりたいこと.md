# 簡単なアプリを作ってjsを学ぶ

## ルール

- AIを使わない、AIで書かない、AIで調べない

### todoアプリ(チェックリスト、ドラッグ&ドロップ)

- html
  - 入力したものをリストで表示
  - リストをドラッグアンドドロップ
  - チェックつけれる

- js
  - 入力を受け取って、配列にして、リストに返す
  - eにドラッグイベントがあったのと、map()は配列を処理して配列で返すらしいから使えそう
  - チェックつける、true/false、js型ないやん

### ストップウォッチ(スタート/ストップ/ラップ)

- html
  - 経過時間を 00:00.00 で表示
  - スタート・ストップ・リセット・ラップのボタン
  - ラップ時間を下に並べる

- js
  - 一定間隔で数字を増やし続ける仕組みと、それを止める仕組みが要る
  - 経過ミリ秒を分・秒に割り算と余りで分解する
  - 0埋め（1 → 01）は文字列側の機能でできるらしい

### 割り勘計算機

- html
  - 合計金額と人数を入力
  - 1人あたりの金額を表示
  - 端数を「切り上げ / 切り捨て」で選べる

- js
  - input の値は文字列なので数値に変換する
  - 人数0や空文字のときに壊れないようにガードする
  - 小数の丸めは切り上げ・切り捨てで関数が違う
  - 3桁区切りのカンマ表示

### メモ帳(保存されるやつ)

- html
  - テキストを書いて保存ボタン
  - リロードしても消えない
  - 削除ボタン

- js
  - ブラウザにデータを残す仕組みを使う
  - 配列やオブジェクトはそのまま保存できないので文字列に変換して、読むときに戻す
  - 保存されているものが無い1回目をどう扱うか

### 名簿検索(インクリメンタルサーチ)

- html
  - 名前の一覧を最初から表示
  - 検索ボックスに打つたびに絞り込まれる
  - ヒット0件のときはメッセージ

- js
  - 打つたびに発火するイベントを使う（クリックではない）
  - 条件に合うものだけ残す配列の処理（todo で map を使ったので今回は別のやつ）
  - 大文字小文字を無視して部分一致

### 買い物リスト(個数と合計金額)

- html
  - 商品名・単価・個数を入力して追加
  - 各行に小計、下に合計
  - 行ごとに削除

- js
  - 配列を1つの値（合計）にまとめる処理
  - 削除は「その行が何番目か」を持たせておく（要素に自前のデータを持たせる方法がある）
  - 行が増えるたびにイベントを付け直さない方法（親でまとめて受け取る）

### じゃんけん(戦績つき)

- html
  - グー・チョキ・パーのボタン
  - 相手の手と勝敗を表示
  - 勝ち/負け/あいこの回数を表示

- js
  - ランダムな手の選び方
  - 勝敗判定を if の羅列以外で書けないか（オブジェクトに「何に勝つか」を持たせる等）
  - 戦績はオブジェクトのプロパティを増やして数える

### 神経衰弱(カードめくり)

- html
  - 裏向きのカードを並べる
  - 2枚めくって同じなら開いたまま、違えば少し待って裏に戻る
  - 全部揃ったらクリア表示

- js
  - 配列をシャッフルする
  - 「今めくっている枚数」の状態管理、めくり中はクリックを無効にする
  - 少し待ってから戻す（タイマーを1回だけ実行するやつ）

### 画像スライダー

- html
  - 画像を1枚表示、左右の矢印で切り替え
  - 下にドット、クリックでその画像へ
  - 一定時間で自動送り

- js
  - 今何枚目かを数字で持つ
  - 最後の次が最初に戻るのは余り算でいける
  - 表示の切り替えはクラスの付け外しで

### タブ切り替え + アコーディオン

- html
  - タブを押すと対応する中身だけ表示
  - 見出しを押すと開閉するリスト

- js
  - クリックされた要素から、対応する中身をどう見つけるか
  - クラスの付け外し（付いていたら外す、を1行でやるやつ）
  - 押された場所から親をたどる方法

### クイズアプリ(4択・採点つき)

- html
  - 問題文と選択肢4つ
  - 選ぶと正誤を出して次の問題へ
  - 最後に「n / m 問正解」

- js
  - 問題は「問題文・選択肢・正解」を持つオブジェクトの配列で持つ
  - 何問目かを進めていって、最後まで行ったら結果画面
  - 選択肢の描画は配列から回して作る（何番目かも一緒に使う）

### 並び替えできる表

- html
  - 名前・年齢・入社日の表
  - 見出しをクリックで昇順/降順に並び替え
  - どの列で並んでいるか矢印で表示

- js
  - 並び替えの比較の書き方（数値と文字列で違う）
  - 日本語の文字列を正しく比べる方法
  - 同じ列をもう一度押したら逆順にする状態の持ち方

### 天気/名言表示(data.json を読む)

- html
  - ボタンを押すと外部ファイルの内容を表示
  - 読み込み中の表示、失敗したときのメッセージ

- js
  - ファイルを取ってくる仕組み（すぐには返ってこない＝非同期）
  - 待ってから使う書き方
  - 失敗したときに拾う書き方
